# 공개 관람 화면과 날짜 표시 — 2026-09-14

- `/`에 공개 관람 화면을 직접 배치했다. 비로그인으로 이름, 승인된 누적 거리·시간, 공식 시즌 인증 일수, 이번 주 승인 합계, 공개 설정된 자기소개를 볼 수 있다.
- 서버에서 비로그인 및 미승인 계정에는 승인 행만 합산하고 날짜별 기록·대기 기록·댓글을 제거한다. 최초 HTML과 새로고침 API에 같은 변환을 적용한다. 자기소개는 운영자가 공개한 항목만 기존 공개 콘텐츠 API로 제공한다.
- 승인된 멤버의 상세 캘린더·댓글 및 본인 업로드 관리는 기존 권한을 유지한다. 익명 응원과 보상 광고는 아직 기획 단계다.
- 인증 날짜 선택은 한국어 연·월·일 선택기로 제공하고 제출 확인 날짜도 `2026년 9월 14일`로 표시한다. 저장/API 형식은 ISO 날짜를 유지한다.
- 검색 대표는 `/`이다. 기존 Search Console 확인 태그·canonical·사이트맵을 유지하며 `/4th`와 `/4th/dashboard`는 noindex를 유지한다. 등록 요청은 실제 검색 노출/순위 보장이 아니다.
- 검증: 154개 테스트, TypeScript, 비로그인 공개 팝업/JSON에 상세 기록과 댓글이 없는지 확인. 실제 회원 기록을 테스트 목적으로 생성하지 않았다.
